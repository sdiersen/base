{% extends 'base.html.twig' %}

{% block title %}Event index{% endblock %}

{% block body %}
    <h1>Event index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>ShortTitle</th>
                <th>Description</th>
                <th>ShortDescription</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for event in events %}
            <tr>
                <td>{{ event.id }}</td>
                <td>{{ event.title }}</td>
                <td>{{ event.shortTitle }}</td>
                <td>{{ event.description }}</td>
                <td>{{ event.shortDescription }}</td>
                <td>
                    <a href="{{ path('events_event_show', {'id': event.id}) }}">show</a>
                    <a href="{{ path('events_event_edit', {'id': event.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('events_event_new') }}">Create new</a>
{% endblock %}
