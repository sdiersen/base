{% extends 'base.html.twig' %}

{% block title %}Edit ScheduledEvent{% endblock %}

{% block body %}
    <h1>Edit ScheduledEvent</h1>

    {{ include('events_scheduled_event/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('events_scheduled_event_index') }}">back to list</a>

    {{ include('events_scheduled_event/_delete_form.html.twig') }}
{% endblock %}
