<form method="post" action="{{ path('events_scheduled_event_delete', {'id': scheduled_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scheduled_event.id) }}">
    <button class="btn">Delete</button>
</form>
