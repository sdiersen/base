{% extends 'base.html.twig' %}

{% block title %}New ScheduledEvent{% endblock %}

{% block body %}
    <h1>Create new ScheduledEvent</h1>

    {{ include('events_scheduled_event/_form.html.twig') }}

    <a href="{{ path('events_scheduled_event_index') }}">back to list</a>
{% endblock %}
