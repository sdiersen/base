<form method="post" action="{{ path('gx_gx_class_delete', {'id': gx_class.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gx_class.id) }}">
    <button class="btn">Delete</button>
</form>
