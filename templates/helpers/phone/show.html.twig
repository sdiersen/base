{% extends 'base.html.twig' %}

{% block title %}Phone{% endblock %}

{% block body %}
    <h1>Phone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phone.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ phone.type }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ phone.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('helpers_phone_index') }}">back to list</a>

    <a href="{{ path('helpers_phone_edit', {'id': phone.id}) }}">edit</a>

    {{ include('helpers_phone/_delete_form.html.twig') }}
{% endblock %}
