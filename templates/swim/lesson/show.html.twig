{% extends 'base.html.twig' %}

{% block title %}Lesson{% endblock %}

{% block body %}
    <h1>Lesson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lesson.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lesson.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ lesson.description }}</td>
            </tr>
            <tr>
                <th>Requirements</th>
                <td>{{ lesson.requirements }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('swim_lesson_index') }}">back to list</a>

    <a href="{{ path('swim_lesson_edit', {'id': lesson.id}) }}">edit</a>

    {{ include('swim_lesson/_delete_form.html.twig') }}
{% endblock %}
