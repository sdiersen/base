<form method="post" action="{{ path('swim_scheduled_lesson_delete', {'id': scheduled_lesson.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scheduled_lesson.id) }}">
    <button class="btn">Delete</button>
</form>
