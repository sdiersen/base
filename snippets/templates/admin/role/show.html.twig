{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ role.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ role.description }}</td>
            </tr>
            <tr>
                <th>DisplayName</th>
                <td>{{ role.displayName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_role_index') }}">back to list</a>

    <a href="{{ path('admin_role_edit', {'id': role.id}) }}">edit</a>

    {{ include('admin/role/_delete_form.html.twig') }}
{% endblock %}
